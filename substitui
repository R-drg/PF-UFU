(define (substitui a b lista)
  (define (sub-aux lista ac)
    (if (empty? lista)
        (reverse ac)
        (sub-aux (cdr lista)
                 (if (equal? a (car lista))
                     (cons b ac)
                 (cons (car lista) ac)))))
  (sub-aux lista empty))
