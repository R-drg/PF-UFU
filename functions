#lang racket


#| Substitui numero v2 |#
(define (substitui a b lista)
    (cond
      [(empty? lista) empty]
      [(=(car lista) a) (cons b ( substitui a b (cdr lista)))]
      [else (cons (car lista) (substitui a b (cdr lista)))]))

#| Exemplo:
> (substitui 1 2 '(1 2 3 4))
'(2 2 3 4)
|#